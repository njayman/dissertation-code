FROM python:3.12-slim

WORKDIR /app
RUN pip install --no-cache-dir uv

COPY pyproject.toml uv.lock ./
COPY src/ ./src/
COPY setup.py ./

RUN uv sync --frozen --no-dev

EXPOSE 8001

CMD ["uv", "run", "devmind-cloud"]
