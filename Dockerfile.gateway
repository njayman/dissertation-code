FROM python:3.12-slim

WORKDIR /app
RUN pip install --no-cache-dir uv

COPY pyproject.toml uv.lock ./
COPY src/ ./src/
COPY ppo_policy.pt ./
COPY setup.py ./

RUN uv sync --frozen --no-dev

ENV DEVMIND_CLOUD_URL=http://devmind-cloud:8001
EXPOSE 8000

CMD ["uv", "run", "devmind-gateway"]
