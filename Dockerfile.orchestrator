FROM python:3.12-slim

WORKDIR /app
RUN pip install --no-cache-dir uv

COPY pyproject.toml uv.lock ./
COPY src/ ./src/
COPY ppo_policy.pt ./
COPY setup.py ./

RUN uv sync --frozen --no-dev

EXPOSE 8002

CMD ["uv", "run", "devmind-orchestrator"]
